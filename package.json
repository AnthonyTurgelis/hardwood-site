{
  "name": "hardwood-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static board site for hardwood scouting; ships one shared status-strip module (hardwood.js).",
  "scripts": {
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage"
  },
  "devDependencies": {
    "jsdom": "24.1.3"
  }
}
