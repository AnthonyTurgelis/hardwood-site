name: Product Surface Integrity (disabled)

# RETIRED HOSTED WRITER — 2026-08-19.
# The canonical product source lives in private AnthonyTurgelis/hardwood-cron.
# This public-repo workflow attempted to fetch that private source anonymously,
# so it could not be trusted to restore or validate the reviewed product bundle.
# The existing source-side serialized publisher owns publication/integrity.
# Keep this definition only as durable history; it must not allocate a runner.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired: use the source-side serialized Hardwood publisher.'
